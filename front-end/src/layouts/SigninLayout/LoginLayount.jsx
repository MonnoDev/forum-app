const SigninLayount = ({children}) => {
  return (
    <div>
        <h2>Welcome to Forum</h2>
        <div> Please log in in order to view the forum</div>
        {children}
    </div>
  )
}

export default SigninLayount;